# gabbyh-cal.github.io
Group 7 255 Final Project Site
Key files:
- index.md --> all HTML code
- assets/
    - js/
      - script.js --> all javascript code
    - css/
      - style.css --> all styles
    - charts/
      --> all charts, static and html
    - data/
      --> cleaned data used directly on the site, geodataframes and dataframes
    - images/
      --> all images
    - notebooks+data/
      --> all python notebooks (documentation in them)
      - Data/
        - raw/
        - cleaned/

All python notebooks will mount the current working directory and should run fine as long as it is in
the current structure where the Data/ folder is in the same directory as all the notebooks.
Excuse the capital "D" in the directory name, it is how it is defined in all the notebooks and was
too late to change.

